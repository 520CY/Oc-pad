继续TODO开发。除了原定计划外，请额外完成以下功能迭代和优化任务：

**1. UI/UX 界面与交互升级**
*   **配色优化**：以此前单一的黑白配色为基础进行升级，引入更丰富、现代化的配色方案（如支持多主题或更专业的色彩搭配）。
*   **交互优化**：改善当前的操作逻辑，提升用户体验的便捷性。
*   **设计参考**：如有必要，请参考 `cc-switch` 和 `Cockpit Tools` 的设计风格与交互逻辑。

**2. 核心联动功能开发**
*   **任务目标**：实现 `opencode` 与 `oh-my-opencode` 的联动配置方案。
*   **资料获取**：请利用 Web Search 功能查阅以下官方文档及其他相关权威资料：
    *   https://github.com/anomalyco/opencode
    *   https://github.com/code-yeongyu/oh-my-opencode/blob/dev/README.zh-cn.md
*   **辅助分析**：如果文档信息不足，请读取并分析本地 `~/.config/opencode` 目录下的配置文件结构，以推断联动逻辑。

**3. 应用重命名**
*   将应用程序名称正式变更为 `oc-pad`。

**4. 配置可视化 (GUI) 实现**
*   **功能要求**：废除纯 JSON 手写代码的配置方式。
*   **具体实现**：开发完整的图形化用户界面（GUI），将 `opencode` 和 `oh-my-opencode` 的所有配置项映射为可视化的表单控件（如输入框、开关、选择器等），支持用户直接在界面上进行查看和修改。

**5. 启动加载逻辑优化**
*   **当前问题**：App 启动后界面空白。
*   **预期行为**：App 启动时，应自动读取并加载系统当前正在使用的 `opencode` 和 `oh-my-opencode` 配置，并将其作为默认值回显在 GUI 界面中，确保用户进入应用即刻看到当前状态。